<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= pageTitle || 'Notes App' %></title>
	<link rel="stylesheet" href="/css/base.css">
	<% if (typeof pageStylesheet !== 'undefined' && pageStylesheet) { %>
	<link rel="stylesheet" href="/css/<%= pageStylesheet %>">
	<% } %>
</head>
<body>
	<header class="site-header">
		<div class="app">
			<nav class="nav">
				<a href="/" class="brand">Notes</a>
				<div class="nav-actions">
					<a href="/createNote" class="btn btn--secondary">Add Note</a>
					<a href="/notesList" class="btn btn--ghost">All Notes</a>
				</div>
			</nav>
		</div>
	</header>
	<main class="app">

